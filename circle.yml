test:
  override:
    - echo "Testing hasn't been implemented yet"

deployment:
  testing:
    branch: develop
    commands:
      - rsync -az --force --delete --exclude-from=.rsyncignore -e 'ssh' ./ $SERVER_USER@$SERVER_NAME:$SERVER_PATH:
          environment:
            SERVER_USER: root
            SERVER_NAME: oddserver.se
            SERVER_PATH: /mnt/persist/www/slack-slash-time-command
